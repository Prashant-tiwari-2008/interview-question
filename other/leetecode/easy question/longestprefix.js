function romanToInteger(namelist) {
    let firstword = namelist[0];
    let compoarestring = '';
    for (let i = 0; i < namelist.length; i++) {
        compoarestring = compoarestring + firstword[0]
    }
} 